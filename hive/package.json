{
  "name": "hive-root",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "concurrently -n app,emu -c blue,yellow \"pnpm --filter @hive/app dev\" \"pnpm dev:emulators\"",
    "dev:emulators": "pnpm --filter @hive/functions build && firebase emulators:start --project demo-hive --import packages/functions/emulator-seed",
    "typecheck": "node scripts/check-docs.mjs && pnpm -r --sequential typecheck",
    "test": "pnpm -r --filter '!e2e' test",
    "validate": "pnpm validate:m0 && pnpm validate:m1 && pnpm validate:m2 && pnpm validate:m3",
    "validate:m0": "pnpm typecheck && pnpm test && pnpm --filter e2e test",
    "validate:m1": "HIVE_PROP_GAMES=500 pnpm --filter @hive/engine test",
    "validate:m2": "HIVE_PROP_GAMES=500 pnpm --filter @hive/engine exec vitest run test/expansions-fixtures.test.ts test/property.test.ts",
    "validate:m3": "pnpm --filter @hive/app test && pnpm --filter e2e exec playwright test tests/hotseat.spec.ts && pnpm validate:visual && pnpm validate:ux",
    "validate:visual": "pnpm --filter e2e exec playwright test -c playwright.visual.config.ts visual/gallery.spec.ts",
    "validate:ux": "pnpm --filter e2e exec playwright test -c playwright.visual.config.ts visual/ux.spec.ts"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "firebase-tools": "^14.4.0",
    "typescript": "^5.6.3"
  }
}
