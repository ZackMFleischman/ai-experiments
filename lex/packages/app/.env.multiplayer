# Loaded by `vite --mode multiplayer` (dev/e2e) and `vite build --mode multiplayer`
# (the Firebase Hosting build, T4.9). The default mode stays the firebase-free
# static hot-seat build (T3.12).
VITE_LEX_MODE=full
