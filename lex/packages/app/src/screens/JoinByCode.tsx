// Join-with-a-code entry (lobby): the dialog lives in @parlor/web/lobby-ui —
// fully game-agnostic (routes to /join/{code}; validation happens there).
export { JoinByCodeButton } from '@parlor/web/lobby-ui';
