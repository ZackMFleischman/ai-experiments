{
  "name": "lex-root",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "concurrently -n app,emu -c blue,yellow \"pnpm --filter @lex/app dev\" \"pnpm dev:emulators\"",
    "dev:emulators": "pnpm --filter @lex/functions build && firebase emulators:start --project demo-lex --import packages/functions/emulator-seed",
    "typecheck": "node scripts/check-docs.mjs && node scripts/check-boundaries.mjs && pnpm -r --sequential typecheck",
    "test": "pnpm -r --filter '!lex-e2e' test",
    "validate": "pnpm validate:m0 && pnpm validate:m1 && pnpm validate:m2",
    "validate:m0": "pnpm typecheck && pnpm test && pnpm --filter lex-e2e test",
    "validate:m1": "LEX_PROP_GAMES=1000 pnpm --filter @lex/engine test",
    "validate:m2": "pnpm --filter @lex/dict build && pnpm --filter @lex/dict test"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "firebase-tools": "^14.4.0",
    "typescript": "^5.6.3"
  }
}
