{
  "name": "breakout-root",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "pnpm --filter @breakout/app dev",
    "typecheck": "node scripts/check-docs.mjs && node scripts/check-boundaries.mjs && pnpm -r --sequential typecheck",
    "test": "pnpm -r test",
    "build": "pnpm --filter @breakout/app build && node scripts/check-bundle.mjs",
    "validate:m1": "BREAKOUT_PROP_RUNS=200 pnpm --filter @breakout/engine test",
    "validate:visual": "pnpm --filter breakout-e2e test:visual",
    "native:sync": "pnpm build && pnpm --filter @breakout/app exec cap sync",
    "native:assets": "pnpm --filter @breakout/app exec node scripts/build-native-assets.mjs && pnpm --filter @breakout/app exec capacitor-assets generate --ios --android --iosProject ../../native/ios/App --androidProject ../../native/android/app"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
