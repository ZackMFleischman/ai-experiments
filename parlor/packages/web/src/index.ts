// @parlor/web — firebase init, auth context, push, lobby queries, providers
// (React). react / firebase / MUI are peerDependencies: the consuming game
// provides them. Real surfaces arrive with their porting tasks
// (lex/IMPLEMENTATION.md T4.1); this is the T0.1 placeholder.

/** Placeholder wiring probe. */
export const PARLOR_WEB = { workspace: 'parlor', package: 'web' } as const;
