// @parlor/server — callable shells (create/join/cancel/challenge/respond/
// rematch), notify, forfeit sweep. firebase-admin / firebase-functions are
// peerDependencies provided by the consuming game's functions package. Real
// surfaces arrive with their porting tasks (lex/IMPLEMENTATION.md T4.4);
// this is the T0.1 placeholder.

/** Placeholder wiring probe. */
export const PARLOR_SERVER = { workspace: 'parlor', package: 'server' } as const;
