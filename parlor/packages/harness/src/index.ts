// @parlor/harness — gallery runtime + validate:visual / validate:ux script
// cores, ported from hive's dev/ + scripts (lex/IMPLEMENTATION.md T3.11).
// This is the T0.1 placeholder.

/** Placeholder wiring probe. */
export const PARLOR_HARNESS = { workspace: 'parlor', package: 'harness' } as const;
