# Loaded by `vite --mode multiplayer` (dev/e2e) and `vite build --mode
# multiplayer` (the Firebase Hosting build). The default mode stays the
# firebase-free static hot-seat build (scripts/check-bundle.mjs).
VITE_CHECKERS_MODE=full
