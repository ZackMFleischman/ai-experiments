{
  "name": "stillness-root",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "pnpm --filter @stillness/app dev",
    "typecheck": "node scripts/check-docs.mjs && pnpm -r --sequential typecheck",
    "test": "pnpm -r test",
    "build": "pnpm --filter @stillness/app build && node scripts/check-bundle.mjs",
    "validate:visual": "pnpm --filter stillness-e2e test:visual",
    "native:sync": "pnpm build && pnpm --filter @stillness/app exec cap sync",
    "native:assets": "pnpm --filter @stillness/app exec node scripts/build-native-assets.mjs && pnpm --filter @stillness/app exec capacitor-assets generate --ios --android --iosProject ../../native/ios/App --androidProject ../../native/android/app"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
